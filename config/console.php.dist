<?php

declare(strict_types=1);

// TODO : attention on ne devrait pas accéder à la classe Framework car elle n'est pas dans le package "chiron/core" !!!! Mettre des valeurs en dure ???
return [
    'name' => Chiron\Core\Core::BANNER_LOGO, // Chiron CLI
    'version' => Chiron\Core\Core::VERSION, // TODO : utiliser un numero de version ajouté comme constante public dans la classe Console::class ou alors passer la string 'UNKNOW' qui est la valeur par défaut de Symfoy/Console !!!
    'commands' => [],
];
